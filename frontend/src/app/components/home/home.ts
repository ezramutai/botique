import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [], // No Angular features used, so this is fine
  templateUrl: './home.html',
  styleUrls: ['./home.css']
})
export class Home {}